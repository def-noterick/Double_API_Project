# Double_API_Project
This project is a custom Node.js web application that mashes up data from two different external APIs. It provides a user-friendly front-end form where a user can search for a word's definition while simultaneously pulling recent bird observation data for select US states.
